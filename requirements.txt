fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
streamlit>=1.36.0
plotly>=5.15.0
pytest>=7.4.0
httpx>=0.24.0
numpy>=1.24.0
# Transformer baseline (SmolLM2-135M) and WikiText benchmark loader.
# Both are optional at runtime — the project falls back to mock behaviour
# with a clear log message when they are not installed.
transformers>=4.44.0
datasets>=2.14.0
