# Core runtime
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
streamlit>=1.36.0
plotly>=5.15.0
numpy>=1.24.0

# Test runner
pytest>=7.4.0
httpx>=0.24.0

# Optional (neural + subword). Install these to enable the LSTM,
# Transformer, and BPETokenizer paths. The project degrades gracefully
# when either torch or transformers is absent: neural fit/predict_next
# return deterministic mock outputs and BPE tests skip. Uncomment or
# install separately, e.g.:
#     pip install torch safetensors transformers
#
# torch>=2.1.0
# safetensors>=0.4.0
# transformers>=4.40.0
