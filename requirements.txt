# Core runtime (Python backend only — the UI is a separate React 19 app
# under frontend/; see frontend/README.md).
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
numpy>=1.24.0

# Test runner
pytest>=7.4.0
httpx>=0.24.0

# Optional (neural + subword). Install these to enable the LSTM,
# Transformer, and BPETokenizer paths. The project degrades gracefully
# when either torch or transformers is absent: neural fit/predict_next
# return deterministic mock outputs and BPE tests skip. Uncomment or
# install separately, e.g.:
#     pip install torch safetensors transformers
#
# torch>=2.1.0
# safetensors>=0.4.0
# transformers>=4.40.0
# sentencepiece>=0.2.0    # Alternative to HF BPE — see src/sp_tokenizer.py

# Optional (observability + distributed rate limit). Install these to
# enable the Prometheus /metrics/prom endpoint and the Redis-backed
# rate limiter (shared across uvicorn workers). Both paths are no-ops
# when the packages / REDIS_URL are absent — the existing JSON /metrics
# and in-memory token bucket keep working.
#     pip install prometheus-fastapi-instrumentator redis
#
# prometheus-fastapi-instrumentator>=7.0.0
# redis>=5.0.0
